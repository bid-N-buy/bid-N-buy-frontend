export type BankAccount = {
  bankName: string;
  accountNumber: string;
  accountHolder: string;
};

export type BankAccountDraft = {
  bankName: string;
  accountNumber: string;
  accountHolder: string;
};
